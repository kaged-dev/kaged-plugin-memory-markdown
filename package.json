{
	"name": "@kaged/memory-markdown",
	"description": "Reference project plugin for agent memory — markdown-based entry storage with keyword scoring, recency boost, tag filtering, and auto-facts aggregation",
	"version": "0.1.1",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kaged-dev/kaged-plugin-memory-markdown.git"
	},
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src",
		"kaged-plugin.yaml"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"format": "biome check --write ."
	},
	"dependencies": {
		"@kaged/utils": "^0.0.2"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/bun": "^1.3.0",
		"typescript": "^5.8.0"
	}
}
